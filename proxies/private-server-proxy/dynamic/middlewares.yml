http:
  middlewares:
    strip-first-segment:
      stripPrefixRegex:
        regex:
          - "^/[^/]+"

    add-network-header:
      headers:
        customRequestHeaders:
          X-Network-Name: "private-server-proxy-network"
