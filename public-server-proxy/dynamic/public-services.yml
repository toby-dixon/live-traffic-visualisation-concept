http:
  routers:
    public-communicating-service:
      middlewares:
        - add-network-header
      rule: Host(`public.localhost`)
      service: public-communicating-service
      entryPoints:
        - web

  services:
    public-communicating-service:
      loadBalancer:
        servers:
          - url: "http://public-communicating-service"
